# JoyOrGenie

JoyOrGenie is an intent-to-action personal agent built for the Convex hackathon.

The core product loop is:

`intent → memory → calendar → real-world options → approval → action → outcome → better memory`

Implementation is in progress. See [the ecosystem reuse record](docs/ecosystem-reuse.md)
for the verified Joy system boundaries used by this standalone application.

